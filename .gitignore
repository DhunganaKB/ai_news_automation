# --- Secrets (NEVER commit) -------------------------------------------------
.env
*.env
!.env.example

# --- Generated / local runtime data -----------------------------------------
data/
ingestion/logs/
infra/.run_url
infra/.fetcher_url

# --- Local-only files (real values / drafts; keep out of a public repo) ------
# instruction.html = ORIGINAL run guide with real project ID / URLs.
# (app_run.html is the MASKED, shareable copy and is intentionally NOT ignored.)
instruction.html
medium_article.md
# --- Python -----------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/

# --- OS / editor ------------------------------------------------------------
.DS_Store
*.swp
.idea/
.vscode/

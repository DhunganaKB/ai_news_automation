# Copy to .env and fill in. .env is gitignored — never commit real keys.

# Tavily Search API key (primary news source). Get one at https://tavily.com
# If left blank, the fetcher falls back to Google News RSS automatically.
TAVILY_API_KEY=
